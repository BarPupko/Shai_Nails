{
  "name": "shai-nails-functions",
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && firebase deploy --only functions --project shainails",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^6.0.0",
    "twilio": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "private": true
}
